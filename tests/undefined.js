const { Flagpole } = require('flagpole');

const suite = Flagpole.Suite('undefined')
   .base({
      dev: 'undefined',
      prod: 'undefined',
   });

suite.Scenario('undefined')
   .open('undefined')
   .undefined()
   .assertions(function (response) {
       
   });
